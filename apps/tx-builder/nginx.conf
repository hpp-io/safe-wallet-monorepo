server {
  listen 80;
  server_name _;

  location /tx-builder/ {
    alias /usr/share/nginx/html/tx-builder/;
    try_files $uri $uri/ /tx-builder/index.html;
    add_header Cache-Control "no-cache";
  }

  location = /tx-builder {
    return 302 /tx-builder/;
  }
}
